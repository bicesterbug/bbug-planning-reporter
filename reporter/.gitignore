node_modules/
.next/
.vercel/
.env
.env*.local
*.tsbuildinfo
next-env.d.ts
__pycache__/
*.pyc

# The repo-root .gitignore ignores `lib/` (Python venv convention); that wrongly
# catches our TypeScript control-plane lib. Re-include it.
!lib/
!lib/**
