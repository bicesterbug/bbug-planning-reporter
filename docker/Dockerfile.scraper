# Cherwell Scraper MCP Server
# Implements [foundation-api:NFR-005] - Containerisation

FROM cherwell-base:latest

# Expose MCP server port (SSE transport)
EXPOSE 3001

# Implements [scraper-health-check:FR-002] - Health check via /health endpoint
HEALTHCHECK --interval=30s --timeout=10s --start-period=5s --retries=3 \
    CMD python -c "import httpx; httpx.get('http://localhost:3001/health', timeout=5).raise_for_status()" || exit 1

# Run the MCP server
CMD ["python", "-m", "src.mcp_servers.cherwell_scraper.server"]
